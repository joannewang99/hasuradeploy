table:
  name: events
  schema: public
select_permissions:
  - role: moderator
    permission:
      columns:
        - id
        - action
        - table_name
        - created_at
        - claim_status
        - claim_id
        - entry_id
        - user_id
      filter: {}
    comment: ""
